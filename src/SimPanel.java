// to indicate that this component is a 'panel' area like a graph
// or a chart that gets special layout treatment.  As opposed to
// components like controls (buttons, sliders, etc.) the SimPanel
// components are resized by the LayoutManager to fill the available
// screen area.

public interface SimPanel {
}
